import 'package:flutter/material.dart';

class ComiteFormScreen extends StatelessWidget {
  const ComiteFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ComiteFormScreen'),
      ),
    );
  }
}
