import 'package:flutter/material.dart';

class CreditoScreen extends StatelessWidget {
  const CreditoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CreditoScreen'),
      ),
    );
  }
}
