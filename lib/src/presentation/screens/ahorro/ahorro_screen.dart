import 'package:flutter/material.dart';

class AhorroScreen extends StatelessWidget {
  const AhorroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AhorroScreen '),
      ),
    );
  }
}
