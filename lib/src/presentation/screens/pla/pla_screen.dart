import 'package:flutter/material.dart';

class PLAScreen extends StatelessWidget {
  const PLAScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PLAScreen'),
      ),
    );
  }
}
