import 'package:flutter/material.dart';

class TesoreriaScreen extends StatelessWidget {
  const TesoreriaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TesoreriaScreen'),
      ),
    );
  }
}
